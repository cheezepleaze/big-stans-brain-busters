[project]
name = "big-stans-brain-busters"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "adrian", email = "s3ac0wian@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "polars>=1.43.2",
]

[project.scripts]
big-stans-brain-busters = "big_stans_brain_busters:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
]
